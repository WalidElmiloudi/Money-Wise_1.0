-- Active: 1768299811758@@127.0.0.1@5432@money_wallet
DROP DATABASE IF EXISTS money_wallet;

CREATE DATABASE money_wallet;

CREATE TABLE income(
  id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  montant DECIMAL(10,2),
  date DATE DEFAULT (CURRENT_DATE),
  description TEXT,
  category VARCHAR(255)
);
CREATE TABLE expence(
  id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY ,
  montant DECIMAL(10,2),
  date DATE DEFAULT (CURRENT_DATE),
  description TEXT,
  category VARCHAR(255)
);

ALTER TABLE income ADD userID INT;
ALTER TABLE expence ADD userID INT;

CREATE TABLE "user"(
 id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
 name VARCHAR(25),
 email VARCHAR(100) UNIQUE,
 password TEXT
);