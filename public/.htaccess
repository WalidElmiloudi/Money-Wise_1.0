
<IfModule mod_rewrite.c>
  Options -Multiviews

  RewriteEngine On
  RewriteBase /Money-Wise_1.0/public

  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  
  RewriteRule ^ - [L]

  RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]
</IfModule>